apiVersion: v1
kind: Service
metadata:
  name: video-pipeline
  labels:
    app.kubernetes.io/name: video-pipeline
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: video-pipeline
  ports:
    - {name: http, port: 8080, targetPort: http, nodePort: 30230}
